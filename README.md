# moment

This is a functioning demo of the 'Thought' capture launcher.

In mid thought it allows you to click on a [element icon or otherwise] of the given user interface or device (such as an iPad).

This opens a window with a text box that you can type in or choose one of the icon buttons.

You have the option to type the text thought 'snippet' might be expressed in text or math equation or symbol then when you click a log is capture to classify it.

All text captured is stored local only and allows you to reset the log [delete] or save as you wish.

Feature request: allows markup output of said text for quick capture of links or the entire list of text at once.

Feature request: future versions will have ability to edit the icons / the launch app.

Feature request: Allows math capture of full expressions

Feature request: Allows music notation or 'humming' an audio

Feature request: Allows a 'scratch pad' draw.
